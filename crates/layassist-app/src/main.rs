//! The `layassist` tray applet binary.
//!
//! M0: starts the applet, loads a checkpoint, and wires the overlay. See
//! `PLAN.md` for the milestone breakdown.

#![cfg_attr(test, allow(clippy::unwrap_used, clippy::expect_used, clippy::panic))]

fn main() {
    // M0 placeholder: the tray applet, model load, and hotkey wiring land here.
}
